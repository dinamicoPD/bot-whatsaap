<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Preguntas Frecuentes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">
  <div class="container">
    <h2 class="mb-4">❓ Preguntas Frecuentes</h2>

    <% if (preguntas && preguntas.length > 0) { %>
      <div class="row">
        <% preguntas.forEach(pregunta => { %>
          <div class="col-md-6 mb-3">
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= pregunta.pregunta %></h5>
                <p class="card-text"><%= pregunta.respuesta %></p>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="alert alert-info">
        <h4>No hay preguntas frecuentes configuradas</h4>
        <p>Agrega preguntas frecuentes en el archivo <code>data/preguntas_frecuentes.json</code></p>
      </div>
    <% } %>

    <a href="/" class="btn btn-secondary mt-3">← Volver al Panel</a>
  </div>
</body>
</html>